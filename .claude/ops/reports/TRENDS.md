# Agent health trends

One row per report, appended by `agent-coach`. Newest at the bottom.
`net_lines` is the line delta the report's proposals would apply to agent definitions —
sustained positive drift means the definitions are accreting and need a pruning pass.

| window_end | sessions | runs | tool_fail | perm_deny | compactions | RC rate | QA fail rate | proposals | net_lines |
| ---------- | -------- | ---- | --------- | --------- | ----------- | ------- | ------------ | --------- | --------- |
